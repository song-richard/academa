import React from 'react';
import { useMutation } from '@apollo/client';
import { DELETE_CARDSET } from '../utils/queries';

import React from 'react'

export const DeleteCardSet = () => {
  return (
    <div>DeleteCardSet</div>
  )
}
